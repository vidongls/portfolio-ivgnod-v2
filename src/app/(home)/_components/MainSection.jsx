import React from 'react';

const MainSection = () => {
	return (
		<div className="w-screen h-screen bg-purple pt-[104px] flex flex-col items-center justify-center">
			MainSection
		</div>
	);
};

export default MainSection;
